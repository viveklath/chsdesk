<h1>Listing bill_transactions</h1>

<table>
  <tr>
    <th>Bill</th>
    <th>Transaction date</th>
    <th>Email</th>
    <th>Pdf</th>
    <th>User member</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bill_transactions.each do |bill_transaction| %>
  <tr>
    <td><%= bill_transaction.bill_id %></td>
    <td><%= bill_transaction.transaction_date %></td>
    <td><%= bill_transaction.email %></td>
    <td><%= bill_transaction.pdf %></td>
    <td><%= bill_transaction.user_member_id %></td>
    <td><%= link_to 'Show', bill_transaction %></td>
    <td><%= link_to 'Edit', edit_bill_transaction_path(bill_transaction) %></td>
    <td><%= link_to 'Destroy', bill_transaction, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill transaction', new_bill_transaction_path %>
