<%= form_for(@ledger_detail) do |f| %>
  <% if @ledger_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ledger_detail.errors.count, "error") %> prohibited this ledger_detail from being saved:</h2>

      <ul>
      <% @ledger_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_group_code %><br />
    <%= f.number_field :account_group_code %>
  </div>
  <div class="field">
    <%= f.label :ledger_name %><br />
    <%= f.text_field :ledger_name %>
  </div>
  <div class="field">
    <%= f.label :opening_balance %><br />
    <%= f.text_field :opening_balance %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
