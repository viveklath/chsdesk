<h1>Listing ledger_details</h1>

<table>
  <tr>
    <th>Account group code</th>
    <th>Ledger name</th>
    <th>Opening balance</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ledger_details.each do |ledger_detail| %>
  <tr>
    <td><%= ledger_detail.account_group_code %></td>
    <td><%= ledger_detail.ledger_name %></td>
    <td><%= ledger_detail.opening_balance %></td>
    <td><%= link_to 'Show', ledger_detail %></td>
    <td><%= link_to 'Edit', edit_ledger_detail_path(ledger_detail) %></td>
    <td><%= link_to 'Destroy', ledger_detail, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ledger detail', new_ledger_detail_path %>
